export enum GenderEnum {
  Male = 'male',
  Female = 'female',
  Other = 'other',
}

export enum ProviderEnum {
  Local = 'local',
  Google = 'google',
  Facebook = 'facebook',
}

export enum RoleEnum {
  User = 'user',
  Admin = 'admin',
}
