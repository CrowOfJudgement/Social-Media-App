export enum StoreTypeEnum {
  Memory = 'memory',
  Disk = 'disk',
}

export const multer_enum = {
  image: ['image/png', 'image/jpeg', 'image/webp'],
  video: ['video/mp4'],
  pdf: ['application/pdf'],
}
